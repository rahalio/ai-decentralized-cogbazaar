/**
 * ApproveGrantPoolDisbursement handler tests
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: grant-pools
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { describe, it, expect, beforeEach, vi } from "vitest";
import { approveGrantPoolDisbursement } from "@cogbazaar/api-server/domains/grant-pools/handlers";
import type { GrantPoolsDDDDependencies } from "@cogbazaar/api-server/domains/grant-pools";
import { createMockRequest, createMockReply, createMockDependencies, TEST_ORG_ID } from "@cogbazaar/tests/shared";
import { GRANTPOOLS_USE_CASES } from "../__factories__/use-cases.js";

describe("ApproveGrantPoolDisbursement", () => {
  let mockRequest: ReturnType<typeof createMockRequest>;
  let mockReply: ReturnType<typeof createMockReply>;
  let mockDeps: GrantPoolsDDDDependencies;
  const orgId = TEST_ORG_ID;

  beforeEach(() => {
    mockRequest = createMockRequest({ orgId });
    mockReply = createMockReply();
    mockDeps = createMockDependencies<GrantPoolsDDDDependencies>(
      GRANTPOOLS_USE_CASES,
      {
        // Mock use case dependencies here
      }
    );
  });

  it("should return 201 with created entity", async () => {
    const input = {} as any;
    const entity = { id: 'mock-id', orgId } as any;
    const useCaseResult = { data: entity, meta: { correlationId: "test", timestamp: new Date().toISOString() } };
    
    mockRequest.body = input;
    vi.spyOn(mockDeps.executeApproveGrantPoolDisbursement, "execute").mockResolvedValue(useCaseResult);

    await approveGrantPoolDisbursement(mockRequest, mockReply, mockDeps);

    expect(mockDeps.executeApproveGrantPoolDisbursement.execute).toHaveBeenCalledWith(expect.anything());
    expect(mockReply.code).toHaveBeenCalledWith(200);
    expect(mockReply.send).toHaveBeenCalledWith(useCaseResult);
  });

});
