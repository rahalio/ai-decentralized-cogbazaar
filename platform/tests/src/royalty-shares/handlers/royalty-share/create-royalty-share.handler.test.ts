/**
 * CreateRoyaltyShare handler tests
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: royalty-shares
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { describe, it, expect, beforeEach, vi } from "vitest";
import { createRoyaltyShare } from "@cogbazaar/api-server/domains/royalty-shares/handlers";
import type { RoyaltySharesDDDDependencies } from "@cogbazaar/api-server/domains/royalty-shares";
import { createRoyaltyShare as createRoyaltyShareEntity } from "@cogbazaar/factories/royalty-shares";
import { createMockRequest, createMockReply, createMockDependencies, TEST_ORG_ID } from "@cogbazaar/tests/shared";
import { ROYALTYSHARES_USE_CASES } from "../__factories__/use-cases.js";

describe("CreateRoyaltyShare", () => {
  let mockRequest: ReturnType<typeof createMockRequest>;
  let mockReply: ReturnType<typeof createMockReply>;
  let mockDeps: RoyaltySharesDDDDependencies;
  const orgId = TEST_ORG_ID;

  beforeEach(() => {
    mockRequest = createMockRequest({ orgId });
    mockReply = createMockReply();
    mockDeps = createMockDependencies<RoyaltySharesDDDDependencies>(
      ROYALTYSHARES_USE_CASES,
      {
        // Mock use case dependencies here
      }
    );
  });

  it("should return 201 with created entity", async () => {
    const input = {} as any;
    const entity = createRoyaltyShareEntity({ orgId });
    const useCaseResult = { data: entity, meta: { correlationId: "test", timestamp: new Date().toISOString() } };
    
    mockRequest.body = input;
    vi.spyOn(mockDeps.executeCreateRoyaltyShare, "execute").mockResolvedValue(useCaseResult);

    await createRoyaltyShare(mockRequest, mockReply, mockDeps);

    expect(mockDeps.executeCreateRoyaltyShare.execute).toHaveBeenCalledWith(expect.anything());
    expect(mockReply.code).toHaveBeenCalledWith(201);
    expect(mockReply.send).toHaveBeenCalledWith(useCaseResult);
  });

});
